name: maranny_two
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  awesome_dialog: ^3.3.0
  file_picker: ^10.3.10
  image_picker: ^1.2.1
  path_provider: ^2.1.5
  url_launcher: ^6.3.2
  flutter_svg: ^2.2.2
  # ✅ Added for persistent profile storage
  shared_preferences: ^2.3.2
  dio: ^5.9.2
  flutter_secure_storage: ^10.0.0
  geolocator: ^14.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
